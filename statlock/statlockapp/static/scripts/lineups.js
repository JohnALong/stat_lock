function opp_lineup(selectObject) {
    let val = selectObject.value
    console.log("val", val)
    let url = ("#Url").attr("data-url")
    // let id = $(this).attr('id')
    // document.location.href = url.replace('123', val)
    // <form action='{% url 'statlockapp:opplineup' opp_team.id %}' method='GET'>
}